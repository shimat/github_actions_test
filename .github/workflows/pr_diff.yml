name: "PR diff sample"

on:
  pull_request:
    paths:
      - "**/foo.json"

jobs:
  check:
    runs-on: ubuntu-latest
    if: contains(github.event.pull_request.changed_files, 'directory/foo.json')
    steps:
      - run: echo "contain"
