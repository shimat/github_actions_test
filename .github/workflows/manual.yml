name: Manual

on:
  workflow_dispatch:
    inputs:
      deploy_env:
        description: 'デプロイ先の環境 (dev/stg/prod)'
        required: true

jobs:
  build:
    name: Deploy to ${{ github.event.inputs.deploy_env }}
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo ${{ github.event.inputs.deploy_env }}
