name: Manual ${{ github.event.inputs.foo }}

on:
  workflow_dispatch:
    input:      
      foo:
        description: 'Foo'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo ${{ github.event.inputs.foo }}
