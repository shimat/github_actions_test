name: Manual

on:
  workflow_dispatch:
    inputs:      
      foo:
        description: 'Foo'
        required: false
        default: ""

jobs:
  A:
    # name: ${{ github.event.inputs.foo }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "A"
          exit 1

  B:
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.foo == 'B' }}
    steps:
      - run: |
          echo "B"

  C:
    runs-on: ubuntu-latest
    needs: [A, B]
    if: always()
    steps:
      - run: |
          echo "${{ needs.A.result }}"
          echo "${{ needs.B.result }}"
          echo "C"

  D:
    runs-on: ubuntu-latest
    needs: [C]
    steps:
      - run: |
          echo "D"
